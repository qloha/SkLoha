package io.github.qloha.skLoha.skript;

// Placeholder ClassInfos to avoid compile-time dependency issues with Skript's Parser types.
public class ClassInfos {
    // Intentionally left blank. ClassInfo for Cutscene is registered at runtime by the plugin if needed.
}
